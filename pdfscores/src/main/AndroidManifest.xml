<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lelloman.pdfscores">

    <application
        android:name=".PdfScoresApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.pdfscoreslist.view.PdfScoresListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.pdfviewer.PdfViewerActivity" />
        <activity android:name=".ui.collectionapps.CollectionAppsActivity" />

        <provider
            android:name=".publicapi.PublicApiContentProvider"
            android:authorities="${applicationId}.provider"
            android:exported="true" />
    </application>

</manifest>